# Decision log

| When | Decision | Evidence | Effect on CV AUC |
|---|---|---|---|
| Thu 24 Sep | Data layout is `data/training`, `data/test`, `data/sample_submission.csv` (renamed from the browser download name); `DATA_DIR` env var overrides | 0.2 | – |
| Thu 24 Sep | `DATE_FMT = %Y-%m-%d`, strict parsing; all 20,000 alert dates are 10-char ISO strings | 1, 2.1 | – |
| Thu 24 Sep | Timestamps are naive with a time part; keep as is, no timezone conversion. Hours 0–22 and weekdays are flat, so no hour-of-day or weekday features | F00a | – |
| Thu 24 Sep | No duplicate handling: 0 exact duplicate rows. Same-second pairs (1.3% of rows) sit almost entirely inside the pre-alert cluster; a gap of 0 is information, keep all rows | F00, 2.5 | – |
| Thu 24 Sep | No transactions after the alert date in train or test (0 rows with lag < 0), so the strict-pre-alert vs all question is moot. Transactions are observed within a common 180-day lookback window (earliest transaction 180 days out for the median alert, at least 170 for 95%) | 2.5, F00b | – |
| Thu 24 Sep | Pre-alert cluster: about 8% of rows (median 31 per alert) fall in the last 3 minutes before the alert midnight; 152 alerts have none; 21 alerts carry it late on the alert day itself. Window `lag_days <= 1` covers all cases | 2.5, F00b | tbd |
| Thu 24 Sep | Window candidates for Phase 4: cluster (`lag_days <= 1`), 7, 30, 90, 180 days, plus recent-vs-baseline ratios; background activity declines from ~3 per day at 120 days to ~1 per day in the last week | F00b | tbd |
| Thu 24 Sep | `VAL = raw`: the index is one global scale (not per-alert standardized), near-normal with a right tail, floor at −2.91, type- and direction-dependent means; test `exp(index)` for money-like sums in Phase 4 | 2.6, F00c | tbd |
| Thu 24 Sep | Amounts are capped per type: the only 4 exactly repeated values are the per-type maxima (card 4.183, cash 4.863, international 6.430, transfer 6.692). Hit-the-cap flag is a Phase 4 candidate; 120 alerts hit a cap, escalation 13% vs 17% base, within noise | 2.6 | tbd |
| Thu 24 Sep | IDs, file row order, transaction-file position and alert date carry no signal (AUC 0.498–0.508); never used as features anyway | 2.7 | – |
| Thu 24 Sep | No shared histories: 0 transaction rows appear under more than one alert, so no pseudo-customers, no group CV, no linkage features | 2.8 | – |
| Thu 24 Sep | CV plan to confirm in Phase 2: stratified K-fold. Test IDs and dates are interleaved with train (30% test share in every id decile and every quarter); 2,405 positives allow 5 folds | 2.7 | – |
| Fri 25 Sep | Split frozen: stratified 80/20, seed 42. Development 11,200 alerts (1,924 escalated), holdout 2,800 (481). One `StratifiedKFold(5, shuffle=True, random_state=42)` inside dev (2,240 alerts, 384–385 escalated per fold); `final_fold` is the same recipe on all 14,000. Saved in `split.csv`. Every label-based view from here on uses dev only | 2.2 | – |
| Fri 25 Sep | Escalation policy is stable: monthly rate on dev 13.6–20.7% with no step or trend (chi-square p = 0.14 over 24 months, Spearman 0.05, 2025 17.0% vs 2026 17.3%, 1 of 24 intervals misses the mean). No time-ordered folds | 2.3, F01 | – |
| Fri 25 Sep | Alert volume moves in steps (up ~60% in Jul 2025, down in Apr and Jul 2026) but alerts per day does not change escalation (AUC 0.49 on dev, every decile interval contains the mean). Not a feature | 2.6, F01 | – |
| Fri 25 Sep | Calendar has no effect on dev: weekday p = 0.45, last 3 days of month p = 0.63, ±3 days of Navruz / Independence Day / hayits p = 0.53. No date or calendar features | 2.7 | – |
| Fri 25 Sep | Test set is the same population as train: adversarial AUC 0.497 on the baseline totals (no feature above 5.4% of gain), weekly test share inside the 95% binomial band in 101 of 105 weeks. Shuffled stratified K-fold confirmed | 2.4, 2.5, F02 | – |
| Fri 25 Sep | Baseline v0: 29 full-history totals (all rows and per direction × type), LightGBM on the dev folds; pooled OOF 0.612. Feature-limited, not model-limited: early stopping keeps 5–86 trees per fold and the best single total (max outgoing transfer) reaches 0.556 alone. Reference model: a feature family is kept when adding it improves this OOF AUC on the frozen folds (a single feature need not beat it alone) | 2.8 | 0.6139 ± 0.0162 |
| Fri 25 Sep | Safety submission `team_486052EC.csv` from the 5 dev-fold models; validator passes (6,000 rows, 0.118–0.324), md5 `6d42902c391c5c8ab23b554996d776df`. TEAM_ID still to be confirmed with the organisers | 2.8 | – |
| Fri 25 Sep | `cv_log.csv` has no timestamp and is rebuilt on every full run; `insights.json` is written in the final Exports cell. Two full runs give identical md5 for every artifact, figure and the submission | 2.8, 6.5 | – |
| Fri 25 Sep | Validation design frozen. Holdout scored once after features, preprocessing, models, hyperparameters and blend are frozen. Warning rule, fixed before scoring: warning if pooled dev OOF AUC − holdout AUC ≥ 2 bootstrap SE of the holdout AUC (≈ 0.025); a warning means investigating on the dev folds and preferring the simpler config, never tuning on the holdout. A warning threshold, not proof of generalization. Final fit: the frozen config on `final_fold` over all 14,000, test predictions averaged over folds | 2.9 | – |
| Fri 25 Sep | Multiple-comparison guard for Phase 3: a single dev AUC has SE 0.0072 under chance (chance band 0.486–0.514); about 120 variables were screened, so only effects well outside the band or consistent across views are acted on | Phase 3 | – |
| Fri 25 Sep | Trigger test answer: neither the trigger level nor its change from the background is predictive on its own. 13 variables at AUC 0.470–0.528, trigger rate ratio 0.494; all 13 together reach dev CV 0.547, below the 0.614 baseline. A compact trigger family goes to the ablation at low priority | 3.4, F04 | 0.547 (trigger only) |
| Fri 25 Sep | Trigger boundary: `lag_days <= 1` for counts, shares and amounts (AUCs within 0.002 of the last-hour core, and it catches the 10 bursts that sit earlier on the day before); the burst core (`secs_before <= 3600`) for timing, where ordinary transactions would stretch a 2.8-minute burst to 135 minutes | 3.5 | – |
| Fri 25 Sep | Background windows: activity curves have the same shape for both classes, escalated alerts about 5% higher at every lag (median ratio 1.055, no trend) and no build-up before the alert. No special window; 7/30/90 days stay default candidates; background counts enter as a level (AUC 0.527) | 3.1, 3.3, F03, F06 | – |
| Fri 25 Sep | Main signal: channel-relative amounts. Escalated alerts make smaller bank transfers (outgoing −0.15, incoming −0.12 channel sd; AUC 0.426 and 0.439) at every lag, card and cash differ by 0.06 sd or less. Against the alert's own card level the gap sharpens: outgoing transfer minus outgoing card size AUC 0.397, deciles from about 24% to 9% escalated. P0 family for Phase 4: per-channel amount levels and cross-channel relative sizes; channel mean/sd/p95 from train+test transactions without labels | 3.8, F09 | tbd |
| Fri 25 Sep | Low priority: burst compression (AUC 0.480–0.514), channel mix and its change (0.476–0.527), transition sequences (within 0.03 of chance; common-pair lift 0.96–1.19). Pass-through leans the expected way but is small (trigger outgoing share 0.528, outflows within 24 h of an inflow 0.526): a small typology family | 3.6, 3.7, 3.9, 3.10, F07, F08, F10, F11 | – |
| Fri 25 Sep | Calendar-time activity: weekly transaction volume follows how many alert windows cover each week; channel mix flat in train and test (card 51–58%, transfers 35–42% of a week). No calendar-time features | 3.2, F05 | – |
| Fri 25 Sep | Error analysis of the most confident misses (3.12) waits for the first behavioural model's dev OOF predictions | 3.12 | – |
